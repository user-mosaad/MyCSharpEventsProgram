# My C# Events Program
Events are used when you want something to happen as a result of an event happening. 

When the event is invoked, that is, when the event calls the function its delegate points to, another function is run as well. So the other function is called a *subscriber* since it subscribes to the event and listens for when the event happens and it runs its code.
